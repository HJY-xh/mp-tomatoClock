<!--pages/focus/focus.wxml-->
<view>
  <text>选择一个事项开始吧</text>
  <view wx:for="{{typeList}}" wx:key="index">
    <mission-card item="{{item}}" data-item="{{item}}" bindtap="onMyEvent"/>
  </view>
</view>